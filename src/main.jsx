import React from 'react';
import { createRoot } from 'react-dom/client';
import * as XLSX from 'xlsx';
import {
  AlertTriangle, BarChart3, Bell, BriefcaseBusiness, CalendarDays, ClipboardCheck,
  Database, Download, Eye, FileDown, FileJson, FileSpreadsheet, Filter, Folder,
  Gauge, GitBranch, Home, KanbanSquare, Languages, Link as LinkIcon, Moon, Plus,
  Printer, RefreshCcw, Search, Settings, Sparkles, Sun, Trash2, Upload, Users,
  Workflow, Layers, Table2, Network, FileText
} from 'lucide-react';
import './styles.css';
import './forms.css';

const LS_KEY = 'pmo-control-hub-records-v9';
const THEME_KEY = 'pmo-theme-mode';
const TYPES = ['projects', 'tasks', 'subtasks', 'risks', 'reports', 'resources', 'schedules', 'files'];
const EMPTY = Object.fromEntries(TYPES.map((type) => [type, []]));
const TODAY = () => new Date().toISOString().slice(0, 10);

const TEXT = {
  en: {
    app: 'Project Control Hub', sub: 'Portfolio, delivery, schedules, resources, files and reports', search: 'Search all records...', upload: 'Import', refresh: 'Refresh', export: 'Export', new: 'New Row', backend: 'Backend', connected: 'Connected', local: 'Local',
    nav: { dashboard: 'Dashboard', portfolio: 'Portfolio', tasks: 'Tasks', subtasks: 'Subtasks', risks: 'Risks', schedule: 'Schedule', resources: 'Resources', reports: 'Reports', files: 'Files', databases: 'Data' },
    labels: { total: 'Projects', tasks: 'Tasks', risks: 'Risks', team: 'Resources', health: 'Health', files: 'Files' }
  },
  ar: {
    app: 'مركز التحكم بالمشاريع', sub: 'إدارة المحفظة والتنفيذ والجداول والموارد والملفات والتقارير', search: 'بحث في كل السجلات...', upload: 'استيراد', refresh: 'تحديث', export: 'تصدير', new: 'صف جديد', backend: 'قاعدة البيانات', connected: 'متصل', local: 'محلي',
    nav: { dashboard: 'الرئيسية', portfolio: 'المشاريع', tasks: 'المهام', subtasks: 'المهام الفرعية', risks: 'المخاطر', schedule: 'الجداول', resources: 'الموارد', reports: 'التقارير', files: 'الملفات', databases: 'البيانات' },
    labels: { total: 'المشاريع', tasks: 'المهام', risks: 'المخاطر', team: 'الموارد', health: 'الصحة', files: 'الملفات' }
  }
};

const MODULES = {
  projects: { page: 'portfolio', icon: BriefcaseBusiness, tone: 'blue', title: 'Project Portfolio', ar: 'محفظة المشاريع', desc: 'Official project register with owner, progress, dates and SharePoint links.', columns: [['en','Project Name','text'],['ar','Arabic Name','text'],['owner','Project Manager','text'],['department','Business Line','text'],['client','Client','text'],['region','Region','text'],['status','Status','select:on,delayed,blocked,done'],['priority','Priority','select:Low,Medium,High,Critical'],['progress','Progress %','number'],['start','Start','date'],['due','Due','date'],['budget','Budget %','number'],['util','Utilization %','number'],['link','Files Link','url']] },
  tasks: { page: 'tasks', icon: ClipboardCheck, tone: 'green', title: 'Task Register', ar: 'سجل المهام', desc: 'Tasks by project, owner, priority and deadline.', columns: [['project','Project','project'],['title','Task','text'],['parent','Parent Task','text'],['owner','Assigned To','text'],['status','Status','select:open,on,delayed,blocked,done'],['priority','Priority','select:Low,Medium,High,Critical'],['progress','Progress %','number'],['due','Due','date'],['notes','Notes','text']] },
  subtasks: { page: 'subtasks', icon: GitBranch, tone: 'green', title: 'Subtask Register', ar: 'المهام الفرعية', desc: 'Detailed subtask breakdown under main tasks.', columns: [['project','Project','project'],['parent','Parent Task','text'],['title','Subtask','text'],['owner','Assigned To','text'],['status','Status','select:open,on,delayed,blocked,done'],['progress','Progress %','number'],['due','Due','date'],['notes','Notes','text']] },
  risks: { page: 'risks', icon: AlertTriangle, tone: 'red', title: 'Risk & Issue Register', ar: 'سجل المخاطر والمشكلات', desc: 'Risks, issues, severity, probability and mitigation.', columns: [['project','Project','project'],['title','Risk / Issue','text'],['owner','Owner','text'],['severity','Severity','select:Low,Medium,High,Critical'],['status','Status','select:open,on,delayed,blocked,done'],['impact','Impact','select:Low,Medium,High,Critical'],['probability','Probability','select:Low,Medium,High,Critical'],['due','Target','date'],['mitigation','Mitigation','text']] },
  schedules: { page: 'schedule', icon: CalendarDays, tone: 'purple', title: 'Schedule & Milestones', ar: 'الجداول والمعالم', desc: 'Plans, phases, milestones and baseline tracking.', columns: [['project','Project','project'],['milestone','Milestone / Phase','text'],['owner','Owner','text'],['start','Start','date'],['due','End','date'],['status','Status','select:on,delayed,blocked,done'],['progress','Progress %','number'],['notes','Notes','text']] },
  resources: { page: 'resources', icon: Users, tone: 'cyan', title: 'Resource Allocation', ar: 'توزيع الموارد', desc: 'Team capacity, planned hours, actual hours and utilization.', columns: [['name','Employee','text'],['role','Role','text'],['department','Department','text'],['project','Project','project'],['capacity','Capacity','number'],['planned','Planned','number'],['actual','Actual','number'],['util','Utilization %','number'],['status','Status','select:Available,Normal,Overloaded,Unavailable']] },
  reports: { page: 'reports', icon: FileSpreadsheet, tone: 'amber', title: 'Status Reports', ar: 'تقارير الحالة', desc: 'Weekly and executive status reports by project.', columns: [['project','Project','project'],['date','Report Date','date'],['health','Health','select:on,delayed,blocked,done'],['achievements','Achievements','text'],['challenges','Challenges','text'],['nextSteps','Next Steps','text'],['support','Support Needed','text']] },
  files: { page: 'files', icon: Folder, tone: 'slate', title: 'Files Register', ar: 'سجل الملفات', desc: 'Documents, OneDrive, Teams and SharePoint folder links.', columns: [['project','Project','project'],['title','File / Folder','text'],['type','Type','select:Schedule,Report,Contract,MOM,Deliverable,Template,Other'],['owner','Owner','text'],['date','Updated','date'],['link','Document Link','url'],['notes','Notes','text']] }
};

const SAMPLE = { source: { type: 'sample', updatedAt: new Date().toISOString() }, projects: [
  { id:'P-001', en:'EPM-IDT Phase 3', ar:'التحول الرقمي للحوكمة', owner:'Mohamed Ahmed', department:'Le Source', client:'EPM', region:'EPM', status:'on', priority:'Medium', progress:70, start:'2024-01-01', due:'2026-12-30', budget:73, util:70, files:12 },
  { id:'P-002', en:'EPM-Cleaning', ar:'الإشراف على النظافة', owner:'Mohamed Idris', department:'Le Source', client:'EPM', region:'EPM', status:'delayed', priority:'High', progress:35, start:'2025-11-03', due:'2028-11-02', budget:82, util:79, files:22 },
  { id:'P-003', en:'EPM-940 Phase 5', ar:'إدارة بلاغات 940', owner:'Mohamed Al Khatem', department:'Le Source', client:'EPM', region:'EPM', status:'on', priority:'Medium', progress:92, start:'2025-12-10', due:'2028-12-09', budget:66, util:82, files:16 }
], tasks: [], subtasks: [], risks: [], resources: [], reports: [], schedules: [], files: [], sheets: [] };

const norm = (v) => v == null ? '' : String(v).trim();
const num = (v, d = 0) => Number.isFinite(Number(v)) ? Number(v) : d;
const titleOf = (r, lang='en') => lang === 'ar' ? (r.ar || r.title || r.name || r.en || r.project || '—') : (r.en || r.title || r.name || r.ar || r.project || '—');
const idOf = (r, type, i = 0) => r._backendId || r._localId || r.id || `${type}-${i}`;
const loadLocal = () => { try { return JSON.parse(localStorage.getItem(LS_KEY)) || EMPTY; } catch { return EMPTY; } };
const saveLocal = (data) => localStorage.setItem(LS_KEY, JSON.stringify(data));
const uid = (type) => `${type.slice(0,1).toUpperCase()}-${Date.now()}`;
const clean = (row) => Object.fromEntries(Object.entries(row).filter(([k]) => !k.startsWith('_')));

async function apiList(type) { const res = await fetch(`/api/records${type ? `?type=${type}` : ''}`, { cache:'no-store' }); const json = await res.json(); if (!res.ok) throw new Error(json.error || 'API failed'); return json.rows || []; }
async function apiCreate(type, record) { const res = await fetch('/api/records', { method:'POST', headers:{ 'Content-Type':'application/json' }, body:JSON.stringify({ type, record }) }); const json = await res.json(); if (!res.ok) throw new Error(json.error || 'API failed'); return json.row; }
async function apiUpdate(id, type, record) { const res = await fetch('/api/records', { method:'PUT', headers:{ 'Content-Type':'application/json' }, body:JSON.stringify({ id, type, record }) }); const json = await res.json(); if (!res.ok) throw new Error(json.error || 'API failed'); return json.row; }
async function apiDelete(id, type) { const res = await fetch('/api/records', { method:'DELETE', headers:{ 'Content-Type':'application/json' }, body:JSON.stringify({ id, type }) }); const json = await res.json(); if (!res.ok) throw new Error(json.error || 'API failed'); return json; }

function downloadText(name, text, mime = 'text/plain') { const blob = new Blob([text], { type:mime }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = name; a.click(); URL.revokeObjectURL(url); }
function rowsForExport(rows) { return rows.map((row) => clean(row)); }
function exportJson(type, rows) { downloadText(`${type}-${TODAY()}.json`, JSON.stringify(rowsForExport(rows), null, 2), 'application/json'); }
function exportCsv(type, rows) { const headers = [...new Set(rowsForExport(rows).flatMap(Object.keys))]; const esc = (v) => `"${String(v ?? '').replace(/"/g, '""')}"`; const body = [headers.join(','), ...rowsForExport(rows).map((r) => headers.map((h) => esc(r[h])).join(','))].join('\n'); downloadText(`${type}-${TODAY()}.csv`, body, 'text/csv'); }
function exportXlsx(type, rows) { const data = rowsForExport(rows); const ws = XLSX.utils.json_to_sheet(data); const wb = XLSX.utils.book_new(); XLSX.utils.book_append_sheet(wb, ws, type.slice(0, 31)); XLSX.writeFile(wb, `${type}-${TODAY()}.xlsx`); }
function exportStyledExcel(type, rows, title = 'PMO Report') { const data = rowsForExport(rows); const headers = [...new Set(data.flatMap(Object.keys))]; const head = headers.map((h) => `<th>${h}</th>`).join(''); const body = data.map((r) => `<tr>${headers.map((h) => `<td>${r[h] ?? ''}</td>`).join('')}</tr>`).join(''); const html = `<html><head><meta charset="utf-8"><style>body{font-family:Segoe UI,Arial}.title{background:#0f2b62;color:white;font-size:22px;font-weight:700;padding:14px}.meta{background:#eef4ff;padding:10px}table{border-collapse:collapse;width:100%}th{background:#2563eb;color:white;font-weight:700;border:1px solid #1d4ed8;padding:8px}td{border:1px solid #dbe3ef;padding:7px}tr:nth-child(even){background:#f8fafc}</style></head><body><div class="title">${title}</div><div class="meta">Generated: ${new Date().toLocaleString()} | Records: ${data.length}</div><table><thead><tr>${head}</tr></thead><tbody>${body}</tbody></table></body></html>`; downloadText(`${type}-formatted-${TODAY()}.xls`, html, 'application/vnd.ms-excel'); }
function exportWordReport(type, rows, title = 'PMO Report') { downloadText(`${type}-report-${TODAY()}.doc`, reportHtml(type, rows, title), 'application/msword'); }
function printPdfReport(type, rows, title = 'PMO Report') { const w = window.open('', '_blank'); if (!w) return; w.document.write(reportHtml(type, rows, title, true)); w.document.close(); w.focus(); }
function reportHtml(type, rows, title, autoPrint = false) { const data = rowsForExport(rows); const avg = data.length ? Math.round(data.reduce((a,r)=>a+num(r.progress),0)/data.length) : 0; const risks = data.filter((r)=>['blocked','delayed','Critical','High'].includes(r.status)||['Critical','High'].includes(r.priority||r.severity)).length; const rowsHtml = data.slice(0, 60).map((r) => `<tr><td>${r.en || r.title || r.project || r.name || ''}</td><td>${r.owner || ''}</td><td>${r.status || r.health || ''}</td><td>${r.progress || ''}</td><td>${r.due || r.date || ''}</td></tr>`).join(''); return `<!doctype html><html><head><meta charset="utf-8"><title>${title}</title><style>@page{size:A4;margin:16mm}body{font-family:Segoe UI,Arial;color:#0f172a}.cover{background:#0f2b62;color:white;padding:24px;border-radius:18px}.cover h1{margin:0;font-size:28px}.kpis{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin:18px 0}.kpi{border:1px solid #dbe3ef;border-radius:14px;padding:14px}.kpi b{font-size:26px}table{width:100%;border-collapse:collapse}th{background:#2563eb;color:white;padding:8px;text-align:left}td{border:1px solid #dbe3ef;padding:7px;font-size:12px}.print{position:fixed;right:20px;top:20px;padding:10px 14px;background:#2563eb;color:white;border:0;border-radius:10px}@media print{.print{display:none}}</style></head><body>${autoPrint ? '<button class="print" onclick="window.print()">Print / Save PDF</button>' : ''}<div class="cover"><h1>${title}</h1><div>${type} | Generated ${new Date().toLocaleString()}</div></div><div class="kpis"><div class="kpi"><span>Records</span><br><b>${data.length}</b></div><div class="kpi"><span>Avg Progress</span><br><b>${avg}%</b></div><div class="kpi"><span>Attention</span><br><b>${risks}</b></div></div><h2>Records</h2><table><thead><tr><th>Name</th><th>Owner</th><th>Status</th><th>Progress</th><th>Due</th></tr></thead><tbody>${rowsHtml}</tbody></table></body></html>`; }

function parseWorkbook(workbook, fileName) { const sheets = workbook.SheetNames.map((name) => { const rows = XLSX.utils.sheet_to_json(workbook.Sheets[name], { defval:'' }); return { name, headers:Object.keys(rows[0] || {}), rows:rows.map(Object.values) }; }); const raw = XLSX.utils.sheet_to_json(workbook.Sheets[workbook.SheetNames.find((x) => /project|solution|erp/i.test(x)) || workbook.SheetNames[0]], { defval:'' }); const projects = raw.map((row, i) => { const v = Object.values(row).map(norm); return { id:v[0] || `P-${i+1}`, department:v[1] || row['Business Line'] || '', client:v[2] || row.Region || '', region:v[2] || row.Region || '', ar:v[3] || row['Arabic Name'] || '', en:v[4] || row['Project Name'] || v[1] || `Project ${i+1}`, owner:v[7] || row['Project Team'] || '', status:'on', priority:'Medium', progress:60, start:v[5] || '', due:v[6] || '', budget:70, util:70 }; }).filter((p)=>p.en && p.en !== 'Project Name'); return { ...SAMPLE, source:{ type:'excel', workbook:fileName, updatedAt:new Date().toISOString() }, sheets, projects: projects.length ? projects : SAMPLE.projects }; }

function Chip({ children, type }) { return <span className={`chip ${type || ''}`}>{children}</span>; }
function Progress({ value }) { const v = Math.max(0, Math.min(100, num(value))); return <div className="progress"><span><i style={{ width:`${v}%` }} /></span><b>{v}%</b></div>; }
function Cmd({ icon: Icon, label, tone = '', onClick }) { return <button className={`commandButton ${tone}`} onClick={onClick}><Icon size={15} /><span>{label}</span></button>; }
function Stat({ icon: Icon, title, value, tone, delta }) { return <div className="statCard"><div className={`statIcon ${tone}`}><Icon size={20} /></div><div><p>{title}</p><strong>{value}</strong>{delta && <small>{delta}</small>}</div></div>; }

function TopBar({ t, lang, setLang, upload, refresh, exportAll, backendState, query, setQuery, theme, toggleTheme }) { const ThemeIcon = theme === 'dark' ? Sun : Moon; return <header className="topBar cleanTop"><div className="brand"><div className="brandLogo"><Workflow size={21} /></div><div><b>{t.app}</b><span>{t.sub}</span></div></div><div className="globalSearch"><Search size={17} /><input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder={t.search} /></div><div className="topActions"><button className="dataSource"><Database size={14} />{t.backend}: {backendState === 'connected' ? t.connected : t.local}</button><label className="uploadBtn"><Upload size={15} />{t.upload}<input type="file" accept=".xlsx,.xls,.xlsm,.csv" onChange={upload} /></label><Cmd icon={RefreshCcw} label={t.refresh} onClick={refresh} /><Cmd icon={FileDown} label={t.export} onClick={exportAll} /><button onClick={() => setLang(lang === 'en' ? 'ar' : 'en')}><Languages size={15} />{lang === 'en' ? 'AR' : 'EN'}</button><button title="Dark / Light" onClick={toggleTheme}><ThemeIcon size={15} />{theme === 'dark' ? 'Light' : 'Dark'}</button><button title="Settings: theme"><Settings size={15} /></button><button><Bell size={15} /></button></div></header>; }
function Sidebar({ t, page, setPage }) { const nav = [['dashboard', Home], ['portfolio', BriefcaseBusiness], ['tasks', ClipboardCheck], ['subtasks', GitBranch], ['risks', AlertTriangle], ['schedule', CalendarDays], ['resources', Users], ['reports', BarChart3], ['files', Folder], ['databases', Database]]; return <aside className="sidebar compactSide"><div className="workspaceCard"><Sparkles size={17} /><div><b>PMO Workspace</b><span>Live control system</span></div></div><nav>{nav.map(([key, Icon]) => <button key={key} className={page === key ? 'active' : ''} onClick={() => setPage(key)}><Icon size={17} /><span>{t.nav[key]}</span></button>)}</nav></aside>; }
function PageHeader({ title, desc, icon: Icon, tone }) { return <section className={`moduleHero compactHero ${tone || 'blue'}`}><div className="moduleIcon"><Icon size={22} /></div><div><h2>{title}</h2><p>{desc}</p></div></section>; }

function Dashboard({ t, records, setPage }) { const projects = records.projects, tasks = [...records.tasks, ...records.subtasks], risks = records.risks, resources = records.resources, files = records.files; const avg = projects.length ? Math.round(projects.reduce((a,p)=>a+num(p.progress),0)/projects.length) : 0; const delayed = projects.filter((p)=>['delayed','blocked'].includes(p.status)).length; return <section className="dashboardPage"><div className="dashboardTitle"><div><h1>Project Control Dashboard</h1><p>Portfolio status, alerts, delivery progress and action lists.</p></div><div className="dashboardBadge">Updated {TODAY()}</div></div><section className="statsGrid compactStats"><button onClick={()=>setPage('portfolio')}><Stat icon={BriefcaseBusiness} title={t.labels.total} value={projects.length} tone="blue" delta="Portfolio" /></button><button onClick={()=>setPage('tasks')}><Stat icon={ClipboardCheck} title={t.labels.tasks} value={tasks.length} tone="green" delta="Open work" /></button><button onClick={()=>setPage('risks')}><Stat icon={AlertTriangle} title={t.labels.risks} value={risks.length || delayed} tone="red" delta="Needs action" /></button><button onClick={()=>setPage('resources')}><Stat icon={Users} title={t.labels.team} value={resources.length} tone="purple" delta="Capacity" /></button><Stat icon={Gauge} title={t.labels.health} value={`${avg}%`} tone="amber" delta="Avg progress" /><button onClick={()=>setPage('files')}><Stat icon={Folder} title={t.labels.files} value={files.length || projects.reduce((a,p)=>a+num(p.files),0)} tone="blue" delta="Documents" /></button></section><section className="dashboardGrid"><div className="panelPro wide"><Gantt projects={projects} /></div><Health projects={projects} /><Bars title="Progress by Department" rows={groupAvg(projects, 'department', 'progress')} /><Alerts projects={projects} setPage={setPage} /></section></section>; }
const groupAvg = (rows, key, metric) => Object.entries(rows.reduce((acc,row)=>{ const name=row[key]||'Other'; (acc[name] ||= []).push(num(row[metric],60)); return acc; },{})).map(([name,arr])=>({ name, value:Math.round(arr.reduce((a,b)=>a+b,0)/arr.length) })).slice(0,7);
function Gantt({ projects }) { return <div className="pm-card noPanel"><div className="panelHeader"><h3>Timeline Overview</h3></div><div className="ganttHead"><span>Project</span><span>%</span><span>Q1</span><span>Q2</span><span>Q3</span><span>Q4</span></div>{projects.slice(0,10).map((p,i)=><div className="ganttRow" key={idOf(p,'projects',i)}><b>{p.en || p.title}</b><em>{p.progress || 0}%</em><div className="ganttTrack"><i className={p.status} style={{ marginLeft:`${(i*8)%35}%`, width:`${Math.max(18, Math.min(70, num(p.progress))) }%` }} /></div></div>)}</div>; }
function Health({ projects }) { const total = projects.length || 1; const counts = ['on','delayed','blocked','done'].map((s)=>projects.filter((p)=>p.status===s).length); return <div className="panelPro pm-card"><div className="panelHeader"><h3>Portfolio Health</h3></div><div className="pulseBody"><div className="donutPro"><span>{total}<small>Total</small></span></div><div className="legendPro"><p><i className="green" />On Track<b>{counts[0]}</b></p><p><i className="amber" />Delayed<b>{counts[1]}</b></p><p><i className="red" />At Risk<b>{counts[2]}</b></p><p><i className="gray" />Done<b>{counts[3]}</b></p></div></div></div>; }
function Bars({ title, rows }) { return <div className="panelPro pm-card"><div className="panelHeader"><h3>{title}</h3></div><div className="miniBars">{rows.map((row)=><p key={row.name}><span>{row.name}</span><i><b style={{ width:`${row.value}%` }} /></i><em>{row.value}%</em></p>)}</div></div>; }
function Alerts({ projects, setPage }) { const items = projects.filter((p)=>['delayed','blocked'].includes(p.status)).slice(0,5); return <div className="panelPro alertsPanel"><div className="panelHeader"><h3>Alerts & Follow-ups</h3><button onClick={()=>setPage('risks')}>View all</button></div>{items.length ? items.map((p)=><div className="alertRow" key={p.id}><AlertTriangle size={16}/><div><b>{p.en}</b><span>{p.owner || 'Owner required'} · Due {p.due || 'TBD'}</span></div><Chip type={p.status}>{p.status}</Chip></div>) : <div className="emptyBox">No critical alerts.</div>}</div>; }

function EditableCell({ col, value, projects, onCommit }) { const [key,, typeDef] = col; const [kind, opts] = typeDef.split(':'); const [draft, setDraft] = React.useState(value ?? ''); React.useEffect(()=>setDraft(value ?? ''), [value]); const save = (v=draft)=>onCommit(key, v); if (kind === 'select') return <select className="cellInput" value={draft} onChange={(e)=>{ setDraft(e.target.value); save(e.target.value); }}><option value="">-</option>{opts.split(',').map((x)=><option key={x} value={x}>{x}</option>)}</select>; if (kind === 'project') return <select className="cellInput" value={draft} onChange={(e)=>{ setDraft(e.target.value); save(e.target.value); }}><option value="">Select project</option>{projects.map((p)=><option key={p.id || p.en} value={p.en || p.name}>{p.en || p.name}</option>)}</select>; return <input className="cellInput" type={kind === 'number' ? 'number' : kind === 'date' ? 'date' : 'text'} value={draft} onChange={(e)=>setDraft(e.target.value)} onBlur={()=>save()} onKeyDown={(e)=>{ if(e.key==='Enter') e.currentTarget.blur(); }} />; }
function moduleFilter(rows, q, filters) { return rows.filter((r)=>{ const matchQ = !q || JSON.stringify(r).toLowerCase().includes(q.toLowerCase()); const matchStatus = !filters.status || filters.status === 'all' || r.status === filters.status || r.health === filters.status; const matchOwner = !filters.owner || filters.owner === 'all' || r.owner === filters.owner; const matchProject = !filters.project || filters.project === 'all' || r.project === filters.project || r.en === filters.project; return matchQ && matchStatus && matchOwner && matchProject; }); }
function unique(rows, key) { return [...new Set(rows.map((r)=>r[key]).filter(Boolean))]; }
function EditableTable({ type, rows, columns, projects, onUpdate, onDelete }) { const visibleColumns = columns.slice(0, type === 'projects' ? 11 : 9); return <section className="panelPro tablePanel editablePanel"><div className="panelHeader"><h3>Editable Table</h3><span>{rows.length} records</span></div><div className="tableWrap"><table className="editableTable"><thead><tr>{visibleColumns.map(([key,label])=><th key={key}>{label}</th>)}<th>Link</th><th>Actions</th></tr></thead><tbody>{rows.map((row,index)=><tr key={idOf(row,type,index)}>{visibleColumns.map((col)=><td key={col[0]}><EditableCell col={col} value={row[col[0]]} projects={projects} onCommit={(key,value)=>onUpdate(type,row,{ [key]: value })} /></td>)}<td>{row.link ? <a className="openLink" href={row.link} target="_blank" rel="noreferrer"><LinkIcon size={14} />Open</a> : <span className="muted">—</span>}</td><td><div className="rowActions"><button className="iconButton"><Eye size={15} /></button>{row._backendId && <button className="iconButton danger" onClick={()=>onDelete(row._backendId,type)}><Trash2 size={15} /></button>}</div></td></tr>)}</tbody></table></div></section>; }
function CardView({ rows, lang }) { return <div className="viewGrid">{rows.map((r,i)=><div className="recordCard" key={idOf(r,'card',i)}><b>{titleOf(r,lang)}</b><span>{r.owner || r.project || r.department || '—'}</span><Chip type={r.status || r.health}>{r.status || r.health || r.priority || 'record'}</Chip><Progress value={r.progress || r.util || 0} /></div>)}</div>; }
function TreeView({ rows, type, lang }) { const groups = rows.reduce((acc,r)=>{ const key = r.project || r.department || r.client || 'Unassigned'; (acc[key] ||= []).push(r); return acc; },{}); return <div className="treeView">{Object.entries(groups).map(([name,items])=><details key={name} open><summary>{name}<span>{items.length}</span></summary>{items.map((r,i)=><p key={idOf(r,type,i)}><GitBranch size={13}/>{titleOf(r,lang)} <em>{r.owner || r.status || ''}</em></p>)}</details>)}</div>; }
function GanttView({ rows }) { return <div className="panelPro pm-card"><div className="panelHeader"><h3>Gantt / Timeline</h3></div>{rows.slice(0,20).map((r,i)=><div className="ganttRow" key={idOf(r,'gantt',i)}><b>{r.en || r.title || r.milestone || r.project || r.name}</b><em>{r.progress || 0}%</em><div className="ganttTrack"><i className={r.status} style={{ marginLeft:`${(i*7)%45}%`, width:`${Math.max(12, Math.min(75, num(r.progress, 40)))}%` }} /></div></div>)}</div>; }

function ModulePage({ type, rows, projects, lang, t, query, onCreate, onUpdate, onDelete }) { const cfg = MODULES[type]; const Icon = cfg.icon; const [view, setView] = React.useState(type === 'schedules' ? 'gantt' : 'table'); const [filters, setFilters] = React.useState({ status:'all', owner:'all', project:'all' }); const filtered = moduleFilter(rows, query, filters); const create = async () => { const blank = { id: uid(type), createdAt:new Date().toISOString() }; if (type !== 'projects' && projects[0]) blank.project = projects[0].en || projects[0].name; await onCreate(type, blank); };
  return <section className={`modulePage ${cfg.tone}`}><PageHeader icon={Icon} tone={cfg.tone} title={lang === 'ar' ? cfg.ar : cfg.title} desc={cfg.desc} /><div className="moduleToolbar"><Cmd icon={Plus} label={t.new} tone="primary" onClick={create} /><Cmd icon={Table2} label="Table" tone={view==='table'?'primary':''} onClick={()=>setView('table')} /><Cmd icon={Layers} label="Cards" tone={view==='cards'?'primary':''} onClick={()=>setView('cards')} /><Cmd icon={Network} label="Tree" tone={view==='tree'?'primary':''} onClick={()=>setView('tree')} /><Cmd icon={KanbanSquare} label="Gantt" tone={view==='gantt'?'primary':''} onClick={()=>setView('gantt')} /><Cmd icon={FileSpreadsheet} label="Excel+" onClick={()=>exportStyledExcel(type, filtered, cfg.title)} /><Cmd icon={FileText} label="Word" onClick={()=>exportWordReport(type, filtered, cfg.title)} /><Cmd icon={Printer} label="PDF" onClick={()=>printPdfReport(type, filtered, cfg.title)} /><Cmd icon={FileJson} label="JSON" onClick={()=>exportJson(type, filtered)} /><Cmd icon={Download} label="XLSX" onClick={()=>exportXlsx(type, filtered)} /></div><div className="filterDock compactFilters"><div><Filter size={14}/><b>Filters</b></div><select value={filters.status} onChange={(e)=>setFilters({...filters,status:e.target.value})}><option value="all">All Status</option>{['open','on','delayed','blocked','done','Available','Normal','Overloaded'].map((x)=><option key={x} value={x}>{x}</option>)}</select><select value={filters.owner} onChange={(e)=>setFilters({...filters,owner:e.target.value})}><option value="all">All Owners</option>{unique(rows,'owner').map((x)=><option key={x} value={x}>{x}</option>)}</select><select value={filters.project} onChange={(e)=>setFilters({...filters,project:e.target.value})}><option value="all">All Projects</option>{projects.map((p)=><option key={p.id||p.en} value={p.en||p.name}>{p.en||p.name}</option>)}</select><span className="filterCount">{filtered.length} / {rows.length}</span></div>{view === 'table' && <EditableTable type={type} rows={filtered} columns={cfg.columns} projects={projects} onUpdate={onUpdate} onDelete={onDelete} />}{view === 'cards' && <CardView rows={filtered} lang={lang} />}{view === 'tree' && <TreeView rows={filtered} type={type} lang={lang} />}{view === 'gantt' && <GanttView rows={filtered} />}</section>; }
function Databases({ t, records, data, query, setQuery }) { const [type, setType] = React.useState('projects'); const rows = moduleFilter(records[type] || [], query, { status:'all', owner:'all', project:'all' }); return <section className="panelPro databasePage"><div className="databaseHero"><div><h3>Data Center</h3><p>Review, filter and export all PMO datasets.</p></div><div className="dbStats"><span><b>{TYPES.length}</b>Lists</span><span><b>{rows.length}</b>Rows</span><span><b>{data.source?.type || 'app'}</b>Source</span></div></div><div className="databaseToolbar"><div className="field"><label>List</label><select value={type} onChange={(e)=>setType(e.target.value)}>{TYPES.filter((x)=>MODULES[x]).map((x)=><option key={x} value={x}>{MODULES[x]?.title || x}</option>)}</select></div><div className="databaseSearch"><Search size={16} /><input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder={t.search} /></div><Cmd icon={FileSpreadsheet} label="Excel+" onClick={()=>exportStyledExcel(type, rows, MODULES[type].title)} /><Cmd icon={FileText} label="Word" onClick={()=>exportWordReport(type, rows, MODULES[type].title)} /><Cmd icon={Printer} label="PDF" onClick={()=>printPdfReport(type, rows, MODULES[type].title)} /></div><EditableTable type={type} rows={rows} columns={MODULES[type].columns} projects={records.projects} onUpdate={()=>{}} onDelete={()=>{}} /></section>; }

function App() { const [lang, setLang] = React.useState('en'); const [page, setPage] = React.useState('dashboard'); const [data, setData] = React.useState(SAMPLE); const [local, setLocal] = React.useState(loadLocal); const [backend, setBackend] = React.useState(EMPTY); const [backendState, setBackendState] = React.useState('local'); const [query, setQuery] = React.useState(''); const [theme, setTheme] = React.useState(() => localStorage.getItem(THEME_KEY) || 'light'); const t = TEXT[lang]; React.useEffect(()=>localStorage.setItem(THEME_KEY, theme), [theme]);
  const records = React.useMemo(() => ({ projects:[...(data.projects || []), ...backend.projects, ...local.projects], tasks:[...backend.tasks, ...local.tasks], subtasks:[...backend.subtasks, ...local.subtasks], risks:[...backend.risks, ...local.risks], resources:[...backend.resources, ...local.resources], reports:[...backend.reports, ...local.reports], schedules:[...backend.schedules, ...local.schedules], files:[...backend.files, ...local.files] }), [data, backend, local]);
  async function refresh() { try { const rows = await apiList(); const next = { ...EMPTY }; rows.forEach((r)=>{ if(next[r.type]) next[r.type].push({ ...r.record, _backendId:r.id, _createdAt:r.created_at }); }); setBackend(next); setBackendState('connected'); } catch { setBackendState('local'); } }
  React.useEffect(()=>{ fetch('/data/sharepoint-data.json', { cache:'no-store' }).then((r)=>r.ok ? r.json() : null).then((j)=>{ if(j) setData((p)=>({ ...p, ...j })); }).catch(()=>{}); refresh(); }, []);
  async function onCreate(type, record) { try { const row = await apiCreate(type, record); setBackend((p)=>({ ...p, [type]: [{ ...row.record, _backendId:row.id }, ...(p[type] || [])] })); setBackendState('connected'); } catch { const next = { ...local, [type]: [{ ...record, _localId:uid(type) }, ...(local[type] || [])] }; setLocal(next); saveLocal(next); setBackendState('local'); } }
  async function onUpdate(type, row, patch) { const updated = { ...row, ...patch, updatedAt:new Date().toISOString() }; if (row._backendId) { setBackend((p)=>({ ...p, [type]: p[type].map((x)=>x._backendId === row._backendId ? updated : x) })); try { await apiUpdate(row._backendId, type, clean(updated)); } catch {} return; } if (type === 'projects' && !row._localId) { setData((p)=>({ ...p, projects:(p.projects || []).map((x)=>(x.id === row.id ? updated : x)) })); return; } const next = { ...local, [type]:(local[type] || []).map((x)=>idOf(x,type) === idOf(row,type) ? updated : x) }; setLocal(next); saveLocal(next); }
  async function onDelete(id, type) { try { await apiDelete(id, type); setBackend((p)=>({ ...p, [type]: p[type].filter((x)=>x._backendId !== id) })); } catch {} }
  async function upload(e) { const file = e.target.files?.[0]; if (!file) return; const buffer = await file.arrayBuffer(); const workbook = XLSX.read(buffer, { type:'array', cellDates:true }); setData(parseWorkbook(workbook, file.name)); setPage('portfolio'); }
  const exportAll = () => exportStyledExcel('pmo-full-export', Object.entries(records).flatMap(([type, rows]) => rows.map((r)=>({ type, ...clean(r) }))), 'PMO Full Export'); const moduleType = { portfolio:'projects', tasks:'tasks', subtasks:'subtasks', risks:'risks', schedule:'schedules', resources:'resources', reports:'reports', files:'files' }[page];
  return <div className={`appShell ${lang === 'ar' ? 'rtl' : 'ltr'} theme-${theme}`} dir={lang === 'ar' ? 'rtl' : 'ltr'}><TopBar t={t} lang={lang} setLang={setLang} upload={upload} refresh={refresh} exportAll={exportAll} backendState={backendState} query={query} setQuery={setQuery} theme={theme} toggleTheme={()=>setTheme(theme === 'dark' ? 'light' : 'dark')} /><Sidebar t={t} page={page} setPage={setPage} /><main className="mainArea">{page === 'dashboard' && <Dashboard t={t} records={records} setPage={setPage} />}{moduleType && <ModulePage type={moduleType} rows={records[moduleType]} projects={records.projects} lang={lang} t={t} query={query} onCreate={onCreate} onUpdate={onUpdate} onDelete={onDelete} />}{page === 'databases' && <Databases t={t} data={data} records={records} query={query} setQuery={setQuery} />}</main></div>; }

createRoot(document.getElementById('root')).render(<App />);
